services:
  - type: web
    name: anna-balla-chatbot
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate"
    startCommand: "gunicorn Chat_Bot.wsgi"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11